# Streamlit Cloud secrets — paste this into your app's Secrets panel
# (App settings → Secrets)
#
# Local development: use .env instead (this file is ignored locally)

SUPABASE_URL      = "https://your-project.supabase.co"
SUPABASE_KEY      = "eyJhbGci..."
ANTHROPIC_API_KEY = "sk-ant-..."
GOOGLE_API_KEY    = "AIza..."

# Ollama is local-only — omit on Streamlit Cloud (slot will show as unavailable)
# OLLAMA_HOST = "http://localhost:11434"
