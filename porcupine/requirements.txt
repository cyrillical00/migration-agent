# Core CLI + display
typer[all]>=0.12
rich>=13.7

# LLM routing
litellm>=1.44
anthropic>=0.40        # needed by litellm for Claude calls
tiktoken>=0.12         # needed by litellm for token counting
tokenizers>=0.22       # needed by litellm
fastuuid>=0.14         # needed by litellm

# Market data
py-clob-client>=0.20
httpx>=0.27,<0.29      # pinned range — supabase + litellm both need this

# Database — install individually; supabase[async] pulls pyiceberg which needs C++ build tools
# Run: pip install supabase-auth supabase-functions storage3==0.8.1 postgrest gotrue realtime supafunc supabase
supabase>=2.7

# Auth
keyring>=25

# Utilities
python-dotenv>=1.0
tenacity>=8.3
